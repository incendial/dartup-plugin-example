name: dart_code_metrics_plugin_loader
description: This pubspec determines the version of the analyzer plugin to load.
version: 1.0.0

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  string_finder_plugin: 
    path: '/Users/dimannich/Desktop/code/dartup-plugin-example'

dev_dependencies:
  lints: ^1.0.1
